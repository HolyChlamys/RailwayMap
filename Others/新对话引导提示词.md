# 新对话引导提示词

复制以下内容作为新对话的开场消息：

---

请先阅读项目文档建立认知：
1. `/home/chlamys/CODE/Project/RailwayMap/CLAUDE.md` — 项目架构
2. `/home/chlamys/CODE/Project/RailwayMap/Others/开发交接手册.md` — 当前进度与待办

当前状态：
- 后端 (:10010) 和前端 (:5173) 均运行中
- PostgreSQL + Redis 在 Docker 中
- 搜索已接入真实 API，车站 14,060 个，铁路线 434K 条
- 默认底图为 MapTiler (天地图因免费 key 429 限流降为备选)

请根据交接手册中"待完成任务"章节按优先级推进开发。关键约束：
- 修改后端 service/common 模块后必须先 `mvn install -DskipTests` 再重启
- 前端 `npm run build` 包含类型检查，模板中不能使用 TS 类型注解
- `train_stops.station_id` 为 NULL，后端查询已改用 `station_name` 匹配
- `train_stops.departTime` 大量 NULL，车次经停发车时间会显示 "-"

请先启动服务 (如果未运行)，然后从 Phase 1 地图交互开始。

---